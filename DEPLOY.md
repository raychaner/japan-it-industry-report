# 🚀 GitHub Pages 部署指南

## 📋 部署步骤

### 步骤1：初始化 Git 仓库

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Add Japan IT Industry Report with Docsify website"
```

---

### 步骤2：在 GitHub 创建仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - **Repository name**：`japan-it-industry-report`（或您喜欢的名字）
   - **Description**：`日本IT行业现状和趋势调查报告`
   - **Public** ✅（必须选择 Public）
   - **不要**勾选 "Add a README file"
3. 点击 **Create repository**

---

### 步骤3：推送代码到 GitHub

```bash
# 关联远程仓库（替换成您的用户名和仓库名）
git remote add origin https://github.com/您的用户名/japan-it-industry-report.git

# 推送代码
git branch -M main
git push -u origin main
```

**如果提示需要登录**：
- 使用 GitHub 用户名
- 密码使用 Personal Access Token（不是账号密码）
- 创建 Token：Settings → Developer settings → Personal access tokens

---

### 步骤4：配置 GitHub Pages

1. 进入您的 GitHub 仓库页面
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**
4. 在 **Source** 部分：
   - **Branch**：选择 `main`
   - **Folder**：选择 `/docs` ⚠️ **重要！必须选择 /docs**
5. 点击 **Save**

---

### 步骤5：等待部署完成

- GitHub 会自动开始部署
- 通常需要 1-3 分钟
- 页面会显示部署状态和网站地址

---

## 🌐 访问您的网站

部署成功后，您的网站地址将是：

```
https://您的用户名.github.io/japan-it-industry-report/
```

例如：
- 用户名：`zhangsan`
- 仓库名：`japan-it-industry-report`
- 网址：`https://zhangsan.github.io/japan-it-industry-report/`

---

## 🔄 更新网站内容

当您修改了文档内容，想要更新网站时：

```bash
# 1. 添加更改
git add .

# 2. 提交更改
git commit -m "Update content"

# 3. 推送到 GitHub
git push

# 4. 等待 1-2 分钟，网站自动更新
```

---

## ⚠️ 常见问题

### Q1: 网站显示 404？

**检查清单**：
- ✅ 确认 Pages 设置中 Folder 选择了 `/docs`
- ✅ 确认 `docs/index.html` 文件存在
- ✅ 等待 3-5 分钟让部署完成
- ✅ 清除浏览器缓存（Ctrl+F5）

### Q2: 推送时提示权限错误？

**解决方案**：
- 使用 Personal Access Token 代替密码
- 创建方法：GitHub → Settings → Developer settings → Personal access tokens → Generate new token
- 权限选择：`repo`（完整仓库访问权限）

### Q3: Mermaid 图表不显示？

**解决方案**：
- 等待页面完全加载
- 刷新页面（Ctrl+F5）
- 检查浏览器控制台是否有错误

### Q4: 侧边栏不显示子标题？

**已解决**：
- 已配置 `loadSidebar: false` 和 `subMaxLevel: 3`
- 侧边栏会自动生成并显示3级标题

---

## 📝 部署检查清单

部署前请确认：

- [ ] 已安装 Git
- [ ] 已配置 Git 用户信息（`git config --global user.name` 和 `user.email`）
- [ ] 有 GitHub 账号
- [ ] 已创建 Personal Access Token（如果需要）
- [ ] 确认仓库设置为 Public
- [ ] 确认 Pages 设置中选择了 `/docs` 目录

---

## 🎉 部署成功后

您将获得：

- ✅ 一个公开的文档网站
- ✅ 自动 HTTPS 加密
- ✅ 全球 CDN 加速
- ✅ 无限流量（GitHub Pages 免费）
- ✅ 自动更新（推送代码后自动部署）

---

**祝您部署顺利！** 🚀

如有问题，请参考：
- [GitHub Pages 官方文档](https://docs.github.com/pages)
- [Docsify 官方文档](https://docsify.js.org/)
